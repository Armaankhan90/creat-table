#include <stdio.h>
int main()
{
  int x,y;
  printf("Enter any number:);
  scanf("%d",&x);
  for(y=1;y<=10;y++);
         {
           printf("%d*%d=%d\n",x,y,x*y);
         }
         return 0;
         }
